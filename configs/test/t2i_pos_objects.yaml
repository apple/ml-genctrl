# @package _global_
# For licensing see accompanying LICENSE file.
# Copyright (C) 2025 Apple Inc. All Rights Reserved.

# Test config for T2I pos_objects task using mock model

defaults:
  - /t2i_pos_objects
  - _self_

model_name: mock-t2i  # Use mock instead of real model

# Override output_map to use mock detector
output_map:
  hf_model_name: mock-owlv2  # Use mock detector
